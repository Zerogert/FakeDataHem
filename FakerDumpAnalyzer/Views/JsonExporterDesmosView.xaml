<UserControl x:Class="FakerDumpAnalyzer.Views.JsonExporterDesmosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FakerDumpAnalyzer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">
            <Button Content="ExportPolygons" Command="{Binding ExportPolygons}"></Button>
            <Button Content="ExportPoints" Command="{Binding ExportPoints}"></Button>
            <Button Content="ExportPolyline" Command="{Binding ExportPolyline}"></Button>
        </StackPanel>

        <TextBox Text ="{Binding JsonContent}" TextWrapping="WrapWithOverflow" AcceptsReturn="True" ></TextBox>

        <StackPanel Grid.Column="2" Orientation="Vertical">
            
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding MinX}"></TextBox>
                <TextBlock Text="&lt;= x =&gt;"></TextBlock>
                <TextBox Text="{Binding MaxX}"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding MinY}"></TextBox>
                <TextBlock Text="&lt;= y =&gt;"></TextBlock>
                <TextBox Text="{Binding MaxY}"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding MinZ}"></TextBox>
                <TextBlock Text="&lt;= z =&gt;"></TextBlock>
                <TextBox Text="{Binding MaxZ}"></TextBox>
            </StackPanel>

            <TextBox Text="{Binding Content}"></TextBox>

        </StackPanel>
        
    </Grid>
</UserControl>
