<Window x:Class="FakerDumpAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FakerDumpAnalyzer" xmlns:views="clr-namespace:FakerDumpAnalyzer.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="120" Text="{Binding Host}" ></TextBox>
            <Button Content="Обновить" Command="{Binding RefreshData}"></Button>
        </StackPanel>
        <TabControl Grid.Row="1">
            <TabItem Header="View"/>
            <TabItem Header="Content" DataContext="{Binding ContentViewModel}">
                <TabItem.Content>
                    <views:ContentView></views:ContentView>
                </TabItem.Content>
            </TabItem>
            <TabItem Header="Экспортер в Desmos 3d"  DataContext="{Binding ExporterDesmosViewModel}">
                <TabItem.Content>
                    <views:ExporterDesmos></views:ExporterDesmos>
                </TabItem.Content>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
